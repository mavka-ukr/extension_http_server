взяти біб мавка

http_server = мавка.завантажити_РМв1(ю"./розширення_мавки_http_server.so")

http_server(8080, дія(метод, шлях, заголовки, тіло, відповісти)
  друк(ю"опа, запит " + метод + ю" до " + шлях)
  друк(заголовки)
  відповісти(200, [=], тіло)
кінець)